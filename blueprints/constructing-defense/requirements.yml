# requirements.yml: external dependencies for the constructing-defense blueprint.
# Local roles (role_*, ludus_install_sysmon) are vendored in this source's ansible/roles/.

#roles:
#  - name: badsectorlabs.ludus_adcs
